# 0.2.0 Windows acceptance record

- Date: 2026-08-08
- Plugin commit: recorded by the `0.2.0` release tag
- Obsidian: 1.13.4 (installer 1.12.7)
- Operating system: Windows desktop
- Vault: formal local Vault; a uniquely named temporary note was moved to Obsidian trash after the checks
- Conflicting heading plugin: not enabled
- Automated gate: `npm run check` passed; 12 test files / 68 tests passed
- Coverage gate: 89.23% statements, 79.66% branches, 98.30% functions, 90.94% lines

## Runtime assets

| Asset | SHA-256 |
|---|---|
| `main.js` | `be10186226872f96194ed6c54bf27ec41efde131806259c2eb0bfb7f61a09358` |
| `manifest.json` | `b5a43df536c69b2e9a351a45d1bac56e881f24d160325a1b4d258feb18d059bd` |
| `styles.css` | `10cf6a5511054f3030f3823a735aaf0be0cff4b367820ad3df9bba4bcb6270f8` |

The deployed files matched these candidate hashes byte for byte. Existing `data.json` was excluded from deployment; on reload, schema 1 settings migrated to schema 2 with the selected custom scheme, templates, display mode, maximum level, marker choice, and excluded folders preserved. The pre-deployment plugin directory was copied to a separate recoverable backup.

## Cases passed

- Plugin 0.2.0 reloaded enabled with no captured runtime errors.
- Settings showed four accessible sections, “跟随 Obsidian”, all nine placeholder formats with examples, four expandable built-in cards, the localized migrated custom card, and H1-H6 source-template previews.
- Virtual Live Preview produced `1`, `1.1`, and `2` while leaving the Markdown SHA-256 unchanged.
- `3.14 Pi` was preserved and an ATX-looking line inside an Obsidian `%%` comment was not decorated.
- Conceal mode hid validated `1` and `1.1` prefixes in Live Preview and Reading View, preserved `3.14`, and revealed the stored prefix when the cursor entered its heading line.
- Reading View concealment used an element whose computed display was `none`; the stored source and Outline headings remained unchanged.
- All nine plugin commands were registered.
- The temporary acceptance note was moved to Obsidian trash, and its empty folder was moved to system trash.

## Explicitly open

- U+2060 source markers remain experimental and disabled by default. The cross-tool marker matrix in `docs/ACCEPTANCE.md` was not completed, so this release makes no interoperability claim for enabling markers.
- Mobile, macOS/Linux, third-party themes, pop-out editor behavior, and a 2,000-heading interactive latency run were not accepted here. `isDesktopOnly` remains true.
