# 0.4.1 Windows acceptance record

- Date: 2026-08-09
- Status: accepted
- Candidate: manifest version 0.4.1 from the release working tree; the published tag records the final commit
- Obsidian: 1.12.7 and 1.13.4
- Operating system: Windows desktop
- Vault: `shared-acceptance-vault`, an isolated acceptance Vault
- Theme: default theme
- Automated gate: 21 test files / 97 tests passed before runtime acceptance

## Runtime assets

| Asset | SHA-256 |
|---|---|
| `main.js` | `81d2c26da3fad0f3681d736fb33f091fe6a10fe7aea54dc45154553175279d13` |
| `manifest.json` | `d4734edd6b1de0533d7769fc4ad6ce380b1a86afcb8a95dc1b8e61c616baee04` |
| `styles.css` | `8dad3fdf6407f86066a347b90de44f9ae85b1923d22b01d11fa8ed7081446af7` |

The installed copies matched the local `dist` assets byte for byte. The candidate manifest declared
version 0.4.1, minimum Obsidian version 1.12.7, and `isDesktopOnly: false`.

## Cases passed

- The plugin loaded with ten commands on both Obsidian versions. Live Preview rendered six virtual
  numbers and preserved the source fixture's SHA-256
  `298383155f6ff98c6ac519d750892c6837bdea58e223d8c1ec6c13b202b66d35`.
- Obsidian 1.13.4 used four native settings navigation cards and no custom tab list. Obsidian 1.12.7
  used the accessible four-tab fallback. The language option read `跟随 Obsidian`, and virtual
  numbering and stored-number concealment remained independent controls that could both be enabled.
- The preview modal carried its own scoped class, had an approximately 819-pixel desktop width, and
  reported five changes plus one warning. Cancelling it did not change the file.
- Applying the write produced SHA-256
  `65e2568666f12949e3ca6f39b8a4b68b46a159349e7b413a3e664aec12f6e98d`;
  editor undo restored the exact original hash.
- The settings save state, display decorations, command preview, write, and undo completed without a
  captured plugin error. Global display settings were restored after the run.

## Method and tooling boundary

Computer Use enumerated the visible Obsidian windows, but its state/control calls were unavailable in
this runtime with `node_repl exec context not found`. Interaction evidence therefore used visible
Windows UI automation, renderer inspection through the isolated debugging endpoint, screenshots,
source hashes, and Obsidian runtime state. This limitation is about the automation channel, not a
Heading Numerals failure.

## Explicitly open

- macOS, Linux, third-party themes, and a physical mobile device were not exercised.
- U+2060 source markers remain experimental and disabled by default; no marker-enabled release claim
  is made.
- Folder/Vault batch rollback was not repeated in this UI pass.
- No candidate asset was deployed to the formal Vault during this acceptance pass.
