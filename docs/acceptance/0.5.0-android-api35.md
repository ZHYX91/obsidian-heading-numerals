# 0.5.0 Android API 35 acceptance record

- Date: 2026-08-09
- Status: accepted
- Candidate: manifest version 0.5.0 from the release working tree; the published tag records the final commit
- Android: 15 / API 35, x86_64 emulator `Obsidian_Clean_ZH_API_35`
- Obsidian: 1.12.7
- Vault: `/sdcard/Documents/HeadingNumerals050Acceptance/Heading Numerals 050 Acceptance`, an isolated acceptance Vault
- Chinese IME: Gboard Chinese (Simplified) Pinyin and English
- Chinese IME composition: passed
- Write and cleanup round trip: passed
- Automated gate: 23 test files / 112 tests passed before runtime acceptance

## Runtime assets

| Asset | SHA-256 |
|---|---|
| `main.js` | `225b963332b330d0b949bb1a2191580bf56537489b66713cafa35893486c2cb9` |
| `manifest.json` | `3ed7f5a5b4b7ec59ed67b7c569c98f8f6a05be40d7149caf446d10d0516d860d` |
| `styles.css` | `b7c778e40304c93996fa71d88e99174f5694721013897404a728cbd33f231a3c` |

The emulator copies matched the final local `dist` assets byte for byte. A final cold start loaded
the candidate, rendered virtual heading numbers, preserved fenced code and comments, and opened the
current-note panel with all four summary labels.

## Cases passed

- The current-note control panel fit the phone width without horizontal clipping. Each setting used
  a stacked summary with Global, Current note override, and Effective value labels.
- Real dropdown interaction wrote `heading-numerals-show-virtual: true` through Obsidian, while the
  unrelated `acceptance-keep: preserved` property remained intact. Follow global deleted that
  property. Conceal stored could then be enabled independently as
  `heading-numerals-conceal-stored: true`.
- Restore all deleted every Heading Numerals override and retained the unrelated property. The
  button became disabled once no overrides remained.
- Runtime testing found and closed a panel state defect: after a successful Properties write, the
  loading class is now removed. Consecutive dropdown changes, scrolling, and Restore all remained
  interactive after each re-render.
- On the unnumbered Operations fixture, Write changed SHA-256 from
  `4ca462dca89d84d809f7e79ba3f484ef56bb1a61b170ae7143d25449c7e14546` to
  `cd3dbc9cb8ada2474dc706c0cb0636155218c5e48c20635462df94dc6f76cf5c`.
  Explicit cleanup restored the exact original hash.
- Real Gboard Pinyin displayed the `ce shi` preedit and the `测试` candidate. Committing it changed
  the file to contain `初始文测试本`; Android undo restored the original `初始文本` source.
- The final three acceptance fixtures matched their local isolated copies after cleanup. Logcat
  contained no Heading Numerals fatal exception, uncaught exception, `TypeError`, or `ReferenceError`.

## Method and cleanup

Interaction used visible emulator screenshots, ADB input, Android accessibility hierarchy, exact
file hashing, and logcat. Chinese text was selected from Gboard's real composition candidate bar,
not injected as completed text. After acceptance, the emulator loaded the pre-existing
`clean_zh_gboard` snapshot; no acceptance Vault was retained in that clean snapshot.

## Explicitly open

- This is emulator evidence, not physical Android-device or iOS acceptance.
- Rotation, background memory pressure, vendor keyboards other than Gboard, and third-party mobile
  themes remain open.
- Folder/Vault batch mutation and recovery were not run on Android.
- No candidate asset was deployed to the formal Vault during this acceptance pass.
